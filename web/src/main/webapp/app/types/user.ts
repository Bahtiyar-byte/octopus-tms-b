/**
 * Legacy user types - DO NOT USE
 * These types are kept for backward compatibility only
 * Use types from './core/user.types' instead
 */

// Re-export from core types for backward compatibility
export {
  UserRole,
  User,
  LoginCredentials,
  AuthResponse,
  ResetPasswordRequest,
  ChangePasswordRequest,
  UserStats,
  UserPreferences
} from './core/user.types';