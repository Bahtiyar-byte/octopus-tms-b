import React from 'react';
import { WorkflowBuilderWrapper } from '../../shared/workflows/components/WorkflowBuilderWrapper';

const WorkflowBuilder: React.FC = () => {
  return <WorkflowBuilderWrapper moduleType="shipper" />;
};

export default WorkflowBuilder;